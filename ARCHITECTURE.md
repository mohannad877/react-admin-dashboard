# البنية المعمارية للمشروع (Architecture Overview)

يعتمد مشروع **Admin Dashboard** على مبادئ **Clean Architecture** باستخدام نمط الطبقات (Layered Architecture) لضمان فصل الاهتمامات وقابلية التوسع وسهولة الصيانة والاختبار.

## 🏗️ هيكلية الطبقات (Layered Architecture)

تم تقسيم المشروع إلى 4 طبقات رئيسية (مجلدات داخل `src/`):

### 1. 🖼️ طبقة العرض (Presentation Layer)
- **المجلد:** `src/presentation/`
- **المسؤولية:** تحتوي على مكونات UI، الصفحات (Pages)، والتخطيطات (Layouts). مسؤولة عن التفاعل مع المستخدم وعرض البيانات، ولا تحتوي على أي منطق عمل معقد أو اتصالات مباشرة بخدمات خارجية.
- **التكنولوجيا:** React, Tailwind CSS, Lucide React.

### 2. 🧠 طبقة التطبيق (Application Layer)
- **المجلد:** `src/application/`
- **المسؤولية:** تربط بين طبقة العرض والبنية التحتية. تحتوي على الـ Contexts (مثل `AuthContext`, `TenantProvider`)، والـ Hooks المخصصة، و Redux Slices (إن وُجدت). تدير حالة التطبيق وتتدخل لمعالجة البيانات قبل وصولها للواجهة.
- **التكنولوجيا:** React Context API.

### 3. ⚙️ طبقة البنية التحتية (Infrastructure Layer)
- **المجلد:** `src/infrastructure/`
- **المسؤولية:** تتعامل مع أي شيء خارج حدود التطبيق (External Systems). تحتوي على الـ API Clients (مثل `ApiClientFactory`)، وإعدادات Firebase و Supabase، والـ Repositories التي تعتبر وسيطاً للبيانات.
- **التكنولوجيا:** Firebase Auth, Supabase JS Client, Axios.

### 4. 🧰 الطبقة المشتركة (Shared Layer)
- **المجلد:** `src/shared/`
- **المسؤولية:** تحتوي على الكود المشترك الذي يمكن استخدامه من قبل أي طبقة أخرى دون إحداث دوائر استدعاء (Circular Dependencies). مثل الـ Utilities، الـ Constants، وملفات إعدادات خارجية مثل `sentry.js` و `i18n.js`.

---

## 🗄️ مصادر البيانات (Data Sources)

يستخدم المشروع النمط المزدوج لمرونة التطوير والإنتاج:

| المصدر | الغرض | البيئة | ملاحظات |
|--------|-------|--------|---------|
| **Supabase (PostgreSQL)** | تخزين الكيانات (المستخدمين والمنتجات) | الإنتاج (Production) | يوفر قاعدة بيانات حقيقية يمكن الوثوق بها |
| **Firebase Auth** | إدارة المصادقة والجلسات | الإنتاج (Production) | إدارة أمان تسجيل الدخول وصلاحيات الدخول |
| **JSON Server** | تطوير واختبار سريع (Mock API) | التطوير (Development) | يعمل محلياً ويسرع التطوير دون الحاجة للإنترنت |

> 💡 **ملاحظة:** يتم التبديل بين المصادر عبر متغيرات البيئة (مثل `VITE_DATA_SOURCE=supabase` أو `json-server`). طبقة `Repository` تخفي هذا التعقيد عن باقي التطبيق.

---

## 🔐 نظام المصادقة (Authentication)

يستخدم التطبيق `AuthContext` لمزامنة حالة المصادقة عبر تطبيق React بأكمله.
- يتم الاعتماد على **Firebase Authentication** في بيئة الإنتاج.
- في بيئة التطوير (عند عدم توافر مفاتيح Firebase)، يتم استخدام **نمط محاكاة (Mock Auth)** يسمح للمطور بالدخول ببيانات وهمية لتسريع عجلة التطوير دون تعطيل.
- المسارات المحمية تتطلب المرور عبر المكون `ProtectedRoute` الذي يُعيد توجيه الزائر غير المصرح له إلى مسار `/login`.

---

## 🎭 تعدد المستأجرين (Multi-tenancy)

صُمم التطبيق ليكون منصة White-label، حيث يمكن تغيير الشعار، اسم الشركة، الألوان، وغيرها عبر ملف `public/tenants.json` بناءً على نطاق الوصول (Domain). يتولى المكون `TenantProvider` قراءة هذه التفضيلات وإرسالها لباقي أجزاء التطبيق لتطبيق الثيم المناسب.
